---
title: "GBIF Gap Finder — Development Roadmap"
subtitle: "(gbif.se/gap-finder)"
author: "GBIF Sweden / NRM"
date: "`r format(Sys.Date(), '%B %Y')`"
output:
  html_document:
    toc: true
    toc_float:
      collapsed: false
    toc_depth: 2
    theme: flatly
    css: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
```

> **About this document.** The living development roadmap for GBIF Gap Finder.
> It is organised in three parts:
>
> 1. **Where it is now** — what the tool does and what works today.
> 2. **What's still to come** — planned work, grouped from "next up" to "exploratory."
> 3. **What's been done & why** — completed milestones and the decisions log.
>
> For a project overview, screenshots, and how to run the tool, see the
> [README](README.md). The companion **`TASKS.md`** breaks open work into
> commit-sized, ordered steps.
>
> Status markers: ✅ / `[x]` done · 🚧 in progress · `[ ]` planned · ⏸ deferred.

---

# Part 1 — Where it is now

## What it is

**GBIF Gap Finder** is a plug-and-play R pipeline and Shiny dashboard that lets any
(European) GBIF node generate a systematic gap analysis of their national biodiversity
data — identifying **where**, **when**, and **which species** are under-represented —
and explore the results interactively.

A node points the pipeline at its country's GBIF data, a national taxonomy backbone, and
(optionally) a red list and invasive-species registry, and gets a complete analysis with
minimal configuration.

## What works today (Sweden)

The full pipeline runs end-to-end for Sweden, and the dashboard is live with nine tabs.

**Pipeline** (scripts 01–11, with a 01a/01b, 06a/06b and 09a/09b/09c split):

| Stage | Status | Notes |
|-------|--------|-------|
| Ingestion (01–04) | ✅ | SQL-API cubes (14-col) → parquet, shared EEA grids, GADM admin |
| Validation (05) | ✅ | Parquet + column checks |
| Core summaries (06a/06b) | ✅ | Taxonomy-agnostic aggregations |
| Spatial gaps (07) | ✅ | Coverage, hotspots, zero-data cells |
| Temporal gaps (08) | ✅ | Recency, seasonality, trends |
| Reconciliation (09a) | ✅ | 4-tier matching → **99.8%** occurrence coverage |
| Taxonomic gaps (09b) | ✅ | Missing taxa, threat / establishment-means coverage |
| Scope summaries (09c) | ✅ | Five scopes (all / backbone / threatened / invasive / sensitive) + recent-period layer |
| Integration (10) | ✅ | Combined overview tables |
| App loader (11) | ✅ | Loads pre-computed per-scope files (near pure loader) |
| `targets` DAG | ✅ | Full `tar_make()` |
| Config system | ✅ | Country-aware `configs/config_{CC}.yml`, shared grids |

**Live dashboard tabs:** Overview · Priorities · Spatial · Temporal · Taxonomic ·
**Species of Concern** (Threatened / Invasive / Sensitive sub-tabs) · Publishers ·
Record Types · Data & Sources.

Also live: a **persona "start here"** entry that jumps users to the tabs relevant to their
role; **DOI + citation** for every dataset on the Data & Sources tab; selectable
**"All" / reset** on every taxonomy and kingdom filter; the three-category publisher
scheme (Citizen science · Private sector · Research data); and the Troudet sampling-bias
chart with a dominant-group exclusion filter.

**Four-layer taxonomy:** national backbone (Dyntaxa) + Red List threat status + Invasive
Species Registry + Sensitive Species List, unified via `in_dyntaxa`, `is_invasive`, and
`is_sensitive` flags.

## Current state

- **Version:** v0.4.3. **Correctness round complete (2026-07)** — a full audit reconciled every
  headline number across Overview / Taxonomic / Concern, fixed the miscalculations (see Part 3),
  and added `scripts/12_reconcile.R` as a build-time guardrail. Numbers are clean.
- **Norway:** scaffolded (`config_NO.yml`, feature flags) but not yet run end-to-end.
- **Disclaimer:** Built with AI assistance (Anthropic's Claude).

---

# Part 2 — What's still to come

Ordered from the immediate, near-term work down to exploratory ideas. The current focus is
**Section A (app alignment & polish)** — the analytical scope is essentially complete; what remains in the short term is cosmetic, communication, and cross-tab consistency.

## A. Next up — app alignment & polish

*Cosmetic, communication, and consistency work. This is the active sprint.*

### Calculations & cross-tab alignment
- [x] **Gap / coverage figures reconcile** across Overview, Taxonomic, and Concern — done in the
  2026-07 audit; `scripts/12_reconcile.R` now enforces it on every build.
- [x] **Overview "threatened" matches Concern** — verified (both read `match_summary_full`).
- [x] **Align the `analysis/*.Rmd` reports with the app** — *done 2026-07 (`gap_finder_report_parity.patch`):*
  reports now take threatened / concern counts from `match_summary` (not `tax_by_threat`), keep DD out
  of "threatened," and use the app's 3-category publisher classifier. Verified numerically in R.
- [ ] Decide whether the **publisher dependency map** should respond to the category filter.
  *(The Publishers count 434 vs 419 is not a bug — 419 is the per-group count; "All" = 434.)*

### Look & readability
- [x] Finish the **font-size pass** (readable ~1rem throughout) — *done 2026-07 (`readability_2`): sub-11px chart labels bumped to 11.*
- [x] **Clearer, plainer tab descriptions** on every tab — *done 2026-07 (`framing_1`): per-tab measurement → interpretation → action guide.*
- [x] **Larger, more visible download buttons** — *done 2026-07 (`readability_1`).*
- [x] Make the **stale-cell colour scale** legible — *done 2026-07 (`eaa_cb_maps`): binned + colour-blind-safe.*
- [x] **Log-transformed view** for publisher charts — *done 2026-07 (`readability_2`): optional Linear/Log toggle.*
- [x] Fix the **Record Types pie** — *done 2026-07 (`readability_1`): "minor types" rollup with the breakdown on hover.*
- [ ] **Data download buttons on all tables and maps** — *in progress (2026-07, `framing_2`): CSV export on the concern, priority & publisher tables; Data & Sources table + maps pending.*
- [ ] **Bar titles in downloaded chart images** when > 20 groups.

### Framing & interpretation
- [x] Consistent **measurement → interpretation → action** structure in tab text — *done 2026-07 (`framing_1`): guide strip on the 6 analysis tabs.*
- [x] **Reword spatial staleness** — *already in place; confirmed 2026-07 (checks national/regional data before prioritising resurvey).*
- [x] Frame **single-publisher cells** as infrastructure vulnerability / partnership opportunity — *done 2026-07 (`framing_1`).*
- [x] Label **"Total Swedish Occurrences"** (not "Total Occurrences") — *done 2026-07 (`framing_1`), demonym-aware for the Norway port.*
- [x] Make the **observed-vs-published distinction** visible — *done 2026-07 (`framing_2`): glossary entry + temporal guide note.*
- [x] Short **"how to interpret…"** examples — *done 2026-07: the per-tab "How to read it" guide + glossary.*
- [x] **Hyperlinks in Overview graph panels** to jump to the relevant tab — *done 2026-07 (`framing_2`).*

### Accessibility — EAA compliance (legal obligation)
- [x] **Glossary / "what does this mean?" tooltips** — *done 2026-07 (`gap_finder_eaa_a11y_structure.patch`):*
  accessible `gloss()` tooltips (hover **and** keyboard focus, screen-reader `aria-label`) plus a full
  Glossary in the Methods panel, covering every listed term. Terms kept, not removed.
- [x] **Colour-blind-safe per-chart palettes** — *done 2026-07 (`gap_finder_eaa_cb_palettes.patch`
  + `gap_finder_eaa_cb_maps.patch`):* categorical charts (in-GBIF/missing, threat, establishment) and
  all choropleth maps (RdYlBu — blue = covered/recent, red = gap/stale), dependency map, and the
  Priorities stale map binned. Sensitive map was already CB-safe.
- [x] Logical **header structure**; a **"methods & limitations"** expandable — *done 2026-07
  (`gap_finder_eaa_a11y_structure.patch`):* `h1` → `h2` (59 card titles) → `h3`; native `<details>`
  Methods, limitations & glossary panel on the Overview.

### Taxonomic UX
- [ ] **External taxon links** (Wikipedia / Artfakta) so non-specialists know what a taxon is.
- [ ] **Consistent Latin/English** in the exclusion search (currently "Aves" vs "Birds").
- [ ] Fix **broken links / missing row names** in taxonomic tables.
- [ ] **Colour-code mobilization targets** by higher taxonomy (plant families green, insects
  red, fungi brown) for quick overview.

### Species of Concern — finishing touches
*(The tab itself is built; these are polish items.)*
- [ ] **Cite + link source checklists** (red list, GRIIS, sensitive list) in the tab.
- [ ] Put the **25/50 km generalised-coordinate caution directly on sensitive-species maps**,
  not only in text.
- [ ] **Swedish common names** in the bottom table.
- [ ] **Confirm the April-2026 red list** is the one in use and link it in "About."

### Priorities & Record Types
- [ ] **Document what the Priorities "targets for the next 12 months" are based on.**
- [ ] **Integrate the sampling-bias data into the Priorities tab** (the remaining Priorities-tab
  review item).
- [ ] **Basis of record "last 12 months" per-basis breakdown** — data exists in 09c's
  `basis_recent_<scope>_<grid>.csv`; app integration pending.

### Temporal & Data tab
- [x] **Default to the last *complete* year** on the temporal tab; keep the current partial
  year selectable but visually distinguished (greyed/dashed) so charts don't imply a decline. *(D3)*
  — *done 2026-07-23 (`gap_finder_data_temporal_polish.patch`; redeploy).*
- [x] Rename **"Select Dataset" → "Select Gap Explorer Dataset"** (or similar) on the Data tab.
  — *done 2026-07-23 (`gap_finder_data_temporal_polish.patch`; redeploy).*
- [x] **Confirm sensitive `sensitivity_category` flows end-to-end**, then add it to the species
  scope lookup CSV for the Data tab. — *done 2026-07-23 (`gap_finder_sensitivity_scope_lookup.patch`).*

### Metadata, citation & docs
- [ ] **Recommended citation for the app** (separate from the data DOI, which is done).
- [ ] **Version / contact / data-provenance block** (the DOI part is done; add version + contact).
- [ ] **GitHub documentation + user manual**, including framing and interpretation guidance.

### Calls to action
- [ ] **Direct publisher CTAs**: "Do you hold data for these taxa?", "Can your institution
  help fill these cells?", "Contact GBIF Sweden for publication support."

## B. Technical improvements

*Pipeline and code work — correctness, portability, hygiene. No UI.*

### Pipeline refactor stragglers
- [x] **Migrate remaining computation out of script 11** — *done 2026-07.* Order-trend, overview-last-year, and Troudet computation moved into script 10; script 11 is a **pure loader** (~260 lines lighter). All temporal windows anchor on the cube snapshot, not `year(Sys.Date())`. *(T-R3; `gap_finder_tr3_*` patches; rerun `tar_destroy()` → `tar_make()`.)*
- [x] **Stop 09b re-classifying the backbone** — read 09a's already-classified output instead
  of reloading and re-running `classify_accepted()`. *(T-R5)*
- [x] **Remove `add_yearmonth_cols()`** once all 09c outputs are confirmed to carry year/month. *(T-R6)*
- [x] **Rename `dyntaxa_*` data keys → `backbone_*`** across 09c filename suffixes, the script 11 loader, and `app.R` — *done (app + main keys clean); minor fallback-key mop-up remains in script 11. (T-R7)*

### Data integrity & robustness
- [x] **03 red-list/sensitive join** — *resolved (2026-07):* the Swedish exports keep authorship
  in a separate column, so exact `scientificName` match is correct; canonicalising over-matched
  Dyntaxa hybrids. GRIIS invasive (which does carry authorship) now also restricts to species-rank,
  non-hybrid taxa. Revisit only for a backbone that bundles authorship into `scientificName`. *(T-I1)*
- [x] **02:** warn if `parse_10km()`-derived 50 km codes don't exist in the grid. *(T-I2)*
- [x] **07:** cache the country boundary to avoid the full-grid `st_union`. *(T-I3)*
- [x] **09b/06b `by_order`/`by_family`** — verified clean (2026-07 audit): files are disjoint and
  aggregation is by key; no double-count. *(T-I4)*

### Performance & hygiene
- [x] **02→07:** cache `cellcodes_10km.txt` / `cellcodes_50km.txt`. *(T-A1)*
- [x] **06a:** document or remove the unused `poly_id` column. *(T-A2)*
- [x] **`run.R`:** bring `cli` usage in line with the pipeline scripts. *(T-Q1)*
- [x] Standardise on `|>`; enforce 100-char width; add doc headers. *(T-Q2, 2026-07-23; app.R prose strings + script 11 header left by design)*
- [x] **`publisher_cell_taxonomy` bundle size** — measured 2026-07-23: 6.4% of bundle / ~11 MB gzip, not the heavy item → NOT pre-aggregated (see `claude/finding-bundle-size-2026-07-23.md`).


### Deferred
- [ ] ⏸ **Swedish marine cells** — include the EEZ in the country clip. Pipeline-geometry change needing marine boundary data; cosmetic, not correctness. *(T-D5)*
- [ ] Decide whether to fold the **recency > 10-years** category into Priorities. *(T-D7.3)*

## C. Larger features (planned — scope & timing TBD)

### Norway replication & country abstraction (Phase 2)
- [ ] Fill `config_NO.yml` DOIs; run end-to-end; document Sweden-specific assumptions that break.
- [ ] Taxonomy / red-list **field abstraction** (generic DwC-A mapping, varying threat-status columns).
- [ ] Generic **"bring your own grid"** support (cell-ID field + CRS config; remove EEA-specific assumptions).

### Snapshot-based retrospective — "Gap Trends" (high value)
GBIF publishes complete monthly snapshots in Parquet. This unlocks two things:
- [ ] **Generate cubes from snapshots** (DuckDB/Arrow) instead of SQL-API downloads — a
  self-contained pipeline with no manual download step.
- [ ] **Run across snapshot dates** (e.g. 2023/2024/2025) and add a **"Gap Trends"** tab/report showing whether mobilisation is actually closing gaps (taxonomic coverage, zero-coverage cells,
  stale-cell trends, threatened-species trajectory, publisher diversity).
- [ ] **Reproducibility without Zenodo** — archive code + config + snapshot date, not 30 GB of derived data.
- [ ] Rebuild a **"Filling the Tab over time -track"** vtab from dated snapshots here.

### b3verse cube-stack rewrite
*From the 2026-05 strategic session ("Bundle B"). Recorded as a workstream; scope & timing TBD.*
- [ ] **Migrate the cube schema to the b3verse / b-cubed standard**, so Gap Finder cubes are interoperable with the wider b-cubed tooling ecosystem.
- [ ] **Automate cube downloads via `rgbif::occ_download_sql()`** — replace the current manual SQL-API download step with a programmatic flow.
- [ ] **SQL canonicalisation** — treat the `GROUP BY` query as the cube spec (the query *is* the definition), so a cube is fully reproducible from its SQL.
- *Overlaps with the snapshot retrospective above — both make cube generation automated and self-contained; the snapshot/diff use case is covered there.*

### Auto-generated "key findings" panel (Overview)
- [ ] A top **"what matters"** panel with 4–6 auto-generated findings (largest spatial gaps,most underrepresented groups, key threatened-species gaps, stale areas, publisher-dependency risks) so a policymaker grasps the message without opening eight tabs. *(The persona nav is built; this auto-panel is the remaining piece.)*

### Operations automation
- [ ] Scheduled re-runs (cadence likely **half-yearly**), notification + auto-retry,
  auto-generated reports, deployment on the NRM server.

### Policy outputs layer
- [ ] Annual Sweden **GBF Target 21** brief; EuropaBON / EBV national contribution report; multi-country Target 21 comparison (GBIF Secretariat handoff). Ties to the snapshot retrospective.

## D. Additional ideas (exploratory / future possibility)

- [ ] **CARE / Indigenous-data-governance tab** — flag cells overlapping Indigenous territories (e.g. Sámi), compare data density / recency / publisher diversity, surface in a "data equity" tab. **Must support data sovereignty, not surveil or extract** — needs GIDA-Sápmi consultation and ethical review. The app must not treat Sámi/Indigenous data as just another gap to mobilise.
- [ ] **EcoBon ecosystem cubes** — overlay ecosystem-level cubes to find under-sampled
  ecosystems; start with a data-availability + grid-alignment assessment.
- [ ] **EuropaBON / EBOCC alignment** — position node-level gap analysis as EuropaBON input; map to EBV workflow templates.
- [ ] **Non-EEA / global grids (for other nodes — out of scope for Lena).** ISEA3H hexagonalgrids via `dggridR`, taxonomy without a national backbone, sparse-data handling, and auto-generating a grid from the country boundary where none exists. Retained as a possibility for contributors, off the critical path. *(D12)*
- [ ] **Other analysis extensions** — 1 km resolution (major data-volume implications); PhyloPic silhouettes on charts; species-distribution-model coverage gaps; temporal trend predictions / forecasting; climate-envelope coverage (SMHI / Copernicus); cross-country comparison mode.
- [ ] **Other app features** — in-app PDF report generation; an API endpoint for programmatic gap-metric access.

## E. Standalone mini-projects (separate from the app)

- [ ] **Artportalen pre-1900 records** — quantify hobbyist-digitised specimen records (dated to 1551), link source specimens, flag duplicate / rights issues, so Artportalen can be informed and exports de-duplicated.
- [ ] **Confirm the April-2026 red list** is the one the pipeline uses; link it in "About."


# Part 3 — What's been done & why

## Decisions log

Canonical outcomes from the strategic / stakeholder sessions. These resolve the design
questions raised during the colleague review.

| # | Decision | Outcome |
|---|----------|---------|
| **D1** | Primary users | **Broadest audience** — not just GBIF staff but policy makers, researchers, funders, agencies, publishers. The full communication layer and EAA accessibility are **in scope for v1**, not optional. |
| **D2** | App name & URL | **GBIF Gap Finder**, at **`gbif.se/gap-finder`** (slug `gap-finder`, hyphenated). Tagline: *"Finding the gaps in Sweden's GBIF-mediated data."* |
| **D3** | Current partial year in temporal trends | **Default = last *complete* year**; the partial current year stays available but is not the default, so charts don't show a misleading end-of-series drop. |
| **D4** | Lowest taxonomic level | **Go to family where feasible** (Class/Order aren't meaningful, esp. for plants). Compute is not the constraint — bundle size / sparsity is; measure first, keep family×cell drill-down-only, consult Kevin per group. |
| **D5** | Communication-layer scope for v1 | **Full** (follows from D1). |
| **D6** | Versioned comparison / annual report | **Defer the in-app feature**; it rides on the snapshot retrospective (Part 2 C). |
| **D7** | Invasives taxonomic level | **Species level, single graph** — Order/Family is uninformative. |
| **D8** | Filters | Filtering was broken across tabs (not just Publishers) — **fixed globally**; every filter has an explicit **"All" / reset**. |
| **D9** | Data DOI / provenance | Surface the **GBIF download DOI** in the app (display + copy) so users can cite and retrieve the exact data used. |
| **D10** | Publisher "GBIF Sweden" | **Remove** — placeholder for orphaned/legacy datasets, not a real publisher (still counted in single-publisher-cell relevance). |
| **D11** | Publisher categories | Three categories: **Citizen science · Private sector · Research data** (the latter holding university, museum, herbarium, sequencing — **and** field stations + marine institutes, resolved 2026-06). |
| **D12** | Global grid abstraction (non-Europe) | **Out of scope for Lena.** Focus stays on the EEA reference grid; non-EEA grids are a future possibility for other nodes, off the critical path. |
| **D13** | First-layer navigation | **Build it** — a "Choose your path" persona entry with short per-group explanations. *(Built — persona start-here buttons; a fuller click-through demo remains optional.)* |

## Completed milestones

### Pipeline & architecture
- **Parquet-based pipeline** — single SQL-API cube per resolution (14 columns incl. publisher and dataset), replacing 18 FST files per basis of record.
- **Country-aware architecture** — `data/{CC}/` structure, `configs/config_{CC}.yml`, shared EEA grids in `data/shared/grids/`.
- **09a / 09b / 09c split** with a clean separation of concerns; **script 11 reduced** from ~1700 to ~940 lines toward a pure loader.
- **4-tier taxonomy reconciliation** — occurrence coverage from **31% → 99.8%**; batched Tier-4 API.
- **Four-layer taxonomy** — backbone + red list + invasive + sensitive, unified via flags.
- **Five-scope summaries (09c)** — all / backbone / threatened / invasive / sensitive variants pre-computed, so the app's scope toggles are file lookups; `exclude_orders` applied here.
- **Recent-period cutoff as a pipeline constant** — derived once, saved to `recent_cutoff.rds`, read by all downstream consumers.
- **Backbone kingdom scope filter (T-R4)** — config-gated filter at the cube (06a/06b/08) drops out-of-backbone kingdoms (bacteria/protists) before reconciliation, cleaning the match rate and making Overview totals consistent with the scoped tabs; NA-kingdom rows kept so reptiles survive.
- **`calc_metric()` logging (T-R1)** — now logs the failing expression + error instead of a silent `NA`.
- **Unclassified rank-bucketing** — blank kingdom/class/order coalesced to "Unclassified" through 09b/09c/11, so order-less taxa (e.g. reptiles → Squamata) stop being silently dropped.
- **Integer-overflow fixes**, redundancy cleanup, and shared-utility consolidation into `globals.R`.

### Audit & correctness round (2026-07)
A full reconciliation of every headline number, shipped as pipeline fixes (branch `fix/audit`):

- **`threatened_in_reference`** now sums species instead of counting threat *categories* (was ≤4).
- **"Cells active last year"** counts distinct cells instead of summing monthly counts (~12× fix).
- **Per-cell occurrence total** excludes the synthetic `"all"` basis row (was 2×).
- **"All GBIF" made genuinely all** — `restrict_to_backbone_scope: false`; 09c's `all` scope is no longer keyed to the reconciliation set (overrides part of T-R4). Occurrence tabs now count every kingdom; Taxonomic / Concern stay backbone-relative.
- **Cell staleness anchored to the cube snapshot date** (`get_snapshot_date`), reproducible across 08/09c.
- **50 km grid clipped centroid-in-country** (like 10 km), fixing the structural 100 %-coverage at 50 km.
- **"Poorly sampled"** redefined as bottom-10 % occurrences OR < 3 cells (was a degenerate `< 1`).
- **Concern checklist matching corrected** — exact match for red-list/sensitive; invasive de-duped to species-rank, non-hybrid (490→337).
- **Input guards** — 04 aborts on missing required cube columns; 05 hard-stops on cube cells absent from the grid; `ds$num_rows` fixed.
- **`scripts/12_reconcile.R`** — a build-time guardrail asserting headline numbers agree across layers.

### App
- **Nine live tabs**, including the **Species of Concern** tab (Threatened / Invasive / Sensitive sub-tabs) that replaced the old Threatened tab.
- **Persona "start here"** navigation (jump-to-tab by user group). *(D13)*
- **Troudet exclusion** multi-select (hide dominant taxa like Aves so smaller groups show).
- **Default GBIF-style groups** on the Troudet landing view (curated mixed-rank mapping).
- **Selectable "All" / reset** on every taxonomy and kingdom filter, across all tabs. *(D8)*
- **Publisher overhaul** — GBIF Sweden hidden (D10), three-category classifier (D11),
  `publisher_taxonomy` / `publisher_cell_taxonomy` bundled so the filters populate.
- **DOI + citation** surfaced on Data & Sources; cube + reference DOIs resolved and linked. *(D9)*
- **App-wide font enlargement** and the Overview "threatened" figure single-sourced.
- **Honest-framing pass** — distinguishes "GBIF data gaps" from "biodiversity knowledge gaps" throughout.
- **Fossil-timeline floor fix** — basis timeline floor lowered so pre-1970 specimen history shows.
- **`year_published` retired** — the `lastinterpreted` cast only ever yielded ~2 usable values;  removed from the cube SQL (now 14 columns) and all downstream code. A snapshot-based "published over time" view will replace it.

### Infrastructure & naming
- **CI / Git-LFS Docker hotfix (v0.4.2)** — the demo image was baking the LFS pointer stub instead
  of the ~90 MB bundle; fixed with `lfs: true` on both checkouts plus a build-time guard.
- **Project rename** to `gbif-gap-finder` across all docs and files; the second "Biodiversity Explorer" app removed.
- The **R-package plan was dropped** — the pipeline as-is is sufficient. The env var
  `GBIFGAPS_COUNTRY` is unchanged in code (a separate, optional rename).

### Diagnostics settled (2026-06)
These looked like bugs but were diagnosed as non-bugs or app-side artifacts:
- **Fossil dates are fine** — `FOSSIL_SPECIMEN` spans 1827→2026 (~50 k records); the "flatline" was the app's `year >= 1970` timeline filter, now fixed.
- **Basis classes aren't dropped** — all nine are present in `spatial_gaps`; they vanish from the *pie* only because they're rounding error next to ~119 M human observations (a chart-choice fix, see Part 2 A).
- **Border zero-coverage cells were a clipping artifact** — border cells sitting in Norway/Finland, kept because script 02 clipped against a 1 km outward-buffered boundary. Fixed via centroid-in-country assignment + a `has_data` fallback (T-D7); rides the rerun.

### Diagnostics settled (2026-07 audit)
- **Concern-count inflation was a matching bug, not new species** — canonicalising the red-list/sensitive join collapsed Dyntaxa hybrids ("A × B", species-rank) onto their parent species. Reverted to exact match; invasive de-duped. The original counts (178 sensitive, 4 859 threatened) were right.
- **Publishers 434 vs 419 is not a discrepancy** — 434 = all contributing orgs (Overview); 419 = orgs for a *selected* taxonomic group (Publishers tab); at "All" both are 434.
- **`by_order`/`by_family` do not double-count** (T-I4) — files disjoint, aggregation by key.

## Open questions

1. **Grid bundling** — bundle EEA grids or always download?
2. **GBIF authentication** — how to handle API credentials for cube downloads?
3. **Unicellular organisms** — currently excluded (no backbone coverage) for taxonoic gap; monitor future checklists.
4. **CARE metrics** — what does "CARE-aligned data" mean quantitatively? Needs GIDA-Sápmi consultation.
5. **EcoBon cubes** — data format, resolution, availability; EEA-grid compatibility?

---

*This roadmap is a living document. Updated `r format(Sys.Date(), '%B %d, %Y')`.*
