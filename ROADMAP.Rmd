---
title: "gbifgaps — Development Roadmap"
subtitle: "GBIF Biodiversity Data Gap Analysis Package"
author: "GBIF Sweden / NRM"
date: "`r format(Sys.Date(), '%B %Y')`"
output:
  html_document:
    toc: true
    toc_float:
      collapsed: false
    toc_depth: 3
    theme: flatly
    css: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
```

# Vision

**gbifgaps** is a plug-and-play R package that enables any GBIF node to generate
a systematic gap analysis of their national biodiversity data — identifying where,
when, and which species are under-represented — and to explore the results through
two interactive dashboards:

1. **Gap Analysis Dashboard** — internal tool for data mobilisation priorities
2. **Biodiversity Explorer** — public-facing tool for exploring national occurrence data

The package is designed so that a node can point it at their country's GBIF data,
a national taxonomy backbone, and (optionally) a red list and invasive species registry,
and produce a complete analysis with minimal configuration.


# Current Status

## What Works (Sweden)

The full pipeline runs end-to-end for Sweden:

| Component | Status | Notes |
|-----------|--------|-------|
| Data ingestion (scripts 01–04) | ✅ Complete | SQL API cubes → parquet, shared EEA grids, GADM admin, MJD year_published fix |
| Invasive species integration | ✅ Complete | Swedish Invasive Species Registry (DwC-A), `is_invasive` flag |
| Sensitive species integration | ✅ Complete | SLU Artdatabanken restricted access list (203 spp), `is_sensitive` flag + `sensitivity_category` |
| Validation (script 05) | ✅ Complete | Parquet validation, column checks |
| Core summaries (scripts 06a, 06b) | ✅ Complete | Dual-scope: full + `_dyntaxa` variants; taxonomy flags on species summaries |
| Spatial gaps (script 07) | ✅ Complete | Coverage, hotspots, zero-data cells |
| Temporal gaps (script 08) | ✅ Complete | Recency from parquet, seasonality, trends |
| Taxonomy reconciliation (script 09a) | ✅ Complete | 4-tier matching, `in_dyntaxa` + `is_invasive` + `is_sensitive` flags |
| Taxonomic gaps (script 09b) | ✅ Complete | Missing taxa, threat coverage, establishment means coverage |
| Integration (script 10) | ✅ Complete | Combined overview tables |
| Gap Analysis app (script 11 + Shiny) | ✅ Complete | Dyntaxa/All GBIF toggle, kingdom spatial filter, heatmap scale, about sections |
| Biodiversity Explorer app (script 12 + Shiny) | ✅ Complete | i18n (EN/SV), vernacular names, invasive species tab |
| R Markdown reports (analysis 01–08) | ✅ Complete | 8 reports including establishment means |
| targets pipeline | ✅ Complete | Full DAG with `tar_make()`, download target, parquet-aware |
| Configuration system | ✅ Complete | Country-aware `configs/config_{CC}.yml`, shared grids, invasives + sensitive config |

## Key Technical Achievements

- **Parquet-based pipeline** — single SQL API cube per resolution (16 columns including publisher, dataset, publication date), replacing 18 FST files per basis of record
- **Country-aware architecture** — `data/{CC}/` folder structure, `configs/config_{CC}.yml`, shared EEA grids in `data/shared/grids/`
- **Taxonomy reconciliation** went from 31% to 99.8% occurrence coverage through a 4-tier matching system
- **Four-layer taxonomy**: Dyntaxa backbone + Red List threat status + Invasive Species Registry + Sensitive Species List, unified via `in_dyntaxa`, `is_invasive`, and `is_sensitive` flags
- **Dual-scope summaries** — every core summary (cell, time, order, family, publisher) produced in both full-GBIF and Dyntaxa-scoped variants, with `exclude_orders` config support (e.g. Primates)
- **Dyntaxa/All GBIF scope toggle** — gap metrics only shown when Dyntaxa backbone is active; "All GBIF" mode shows overview without gap metrics
- **Kingdom-level spatial filtering** — recency maps can be filtered by kingdom to reveal patterns hidden by bird-dominated data
- **Publisher analysis** — publisher summary, single-publisher cell dependency, published vs observed time
- **Troudet-style sampling bias** at class, order, and family levels with hierarchical auto-drill
- **Cascading taxonomy filters** (Kingdom→Phylum→Class→Order→Family) across app tabs with active filter breadcrumb
- **Rolling 12-month window** — observations dated vs published to GBIF (year_published via MJD conversion)
- **Heatmap scale toggle** — log, linear, and annotated count views for temporal heatmaps
- **Expandable About descriptions** on every tab, following the GBIF Country Metrics style
- **Administrative boundaries** from GADM with map overlays and regional filtering
- **Swedish/English i18n** in the Explorer app with vernacular species names from Dyntaxa
- **Europe-wide EEA grids** — shared 10km + 50km grids, clipped to country extent automatically


# Phase 1: Sweden Hardening (Current)

*Goal: Ensure the Sweden pipeline is bulletproof before extending to other countries.*

## 1.1 Pipeline Integrity ✅

- [x] Integer overflow fixes across all scripts, Rmds, and Shiny apps
- [x] 09a/09b script split with 4-tier taxonomy reconciliation
- [x] Pipeline housekeeping (README, config, packages.R, _targets.R, run.R)
- [x] Taxonomy QA review report (Rmd 07)
- [x] Full end-to-end `tar_make()` run

## 1.2 Taxonomy Restructuring ✅

- [x] Invasive species registry integration (download, ingest, `is_invasive` flag)
- [x] `in_dyntaxa` flag propagated through reconciliation to app
- [x] Dyntaxa/All GBIF scope toggle in Gap Analysis app
- [x] Kingdom-level cell recency data for spatial filtering
- [x] Config system updated for invasives (SE, NO, template)
- [x] Published Over Time chart fix (year_published type casting)

## 1.3 Sensitive Species & Dual-Scope Summaries ✅

- [x] Sensitive species list integration (SLU Artdatabanken, 203 species)
  - Download via script 01, ingest via script 03
  - `is_sensitive` flag + `sensitivity_category` (5km/25km/50km from taxonRemarks)
  - Propagated through 06a → 06b → 09a → 11 → app
- [x] New GBIF cubes with `year_published` fix
  - GBIF SQL API returns MJD (Modified Julian Date), not integer year
  - Script 04 converts via `as.Date(x, origin = "1858-11-17")`
  - Controlled by `parameters.taxonomic.year_published_format: "mjd"` in config
- [x] Dual-scope summaries in script 06a
  - `write_dual_scope()` produces both full and `_dyntaxa` file variants
  - Taxonomy lookup joined at cube level (species name → `in_dyntaxa`, `is_invasive`, `is_sensitive`)
  - `exclude_orders` config applied at lookup level (e.g. Primates)
- [x] Taxonomy flags carried through species summaries in script 06b
  - `in_dyntaxa`, `is_invasive`, `is_sensitive`, `establishmentMeans` in every output CSV
- [x] Primates exclusion: `parameters.taxonomic.exclude_orders: ["Primates"]` in config + app
- [x] Filter breadcrumb on Taxonomic tab (shows active filter path with clear button)
- [x] Updated all configs (SE, NO, template), globals.R, README

## 1.4 App Tab Review (In Progress)

Tab-by-tab feedback and refinement of the Gap Analysis app:

- [x] Spatial tab — reviewed and complete
- [x] Temporal tab — reviewed and complete
- [x] Basis of Record tab — reviewed and complete
- [ ] Taxonomic tab — in progress:
  - [x] Filter breadcrumb (item 6)
  - [x] Primates exclusion (item 1)
  - [ ] Troudet multi-select exclusion filter (e.g. exclude Aves from bias chart)
  - [ ] Default GBIF-style groups on Troudet landing view (curated mixed-rank mapping)
- [ ] Species of Concern tab — **new tab replacing Threatened** (sub-tabs: Threatened, Invasive, Sensitive)
- [ ] Publishers tab — review pending
- [ ] Priorities tab — review pending (integrate sampling bias data)
- [ ] Overview tab — review pending

## 1.5 Remaining Sweden Work

- [ ] **Species of Concern tab** — merge Threatened + Invasive + Sensitive into one tab with sub-tabs:
  - Threatened sub-tab: lift from current Threatened tab (coverage by CR/EN/VU/NT/DD, missing species table)
  - Invasive sub-tab: monitoring coverage, spatial concentration, temporal trends, over-/under-representation
  - Sensitive sub-tab: breakdown by sensitivity category (5/25/50km), taxonomic distribution, data quality impact
- [ ] **Troudet exclusion filter** — multi-select input to remove dominant groups (e.g. Aves) from bias chart
- [ ] **Default GBIF-style groups** — curated mixed-rank mapping table for Troudet landing view (Birds, Mammals, Insects, Vascular Plants, Fungi, etc.)
- [ ] **Script 11 optimization** — replace cube re-aggregation (section 5d) with pre-computed `_dyntaxa` files from 06a
- [ ] Publisher type classification (citizen science, museums, universities, agencies)
- [ ] Basis of record "last 12 months" per-basis breakdown
- [ ] Climate correlation on temporal tab (SMHI gridded data — deferred to v2)
- [ ] Color blind accessibility audit (per-chart, not wholesale palette swap)
- [ ] Bar titles in downloaded images when >20 groups
- [ ] PhyloPic taxonomic silhouette icons — *parked*
- [ ] 1km grid resolution option — *requires evaluation*

## 1.6 Code Quality

- [ ] Consistent `|>` pipe syntax throughout (currently mixed `|>` and `%>%`)
- [ ] Remove dead code and commented-out sections
- [ ] Standardise function documentation headers
- [ ] Ensure all scripts respect 100-character line width


# Phase 2: Country Abstraction

*Goal: Make the pipeline work for any European country with EEA grids.*

## 2.1 Norway — First Replication

Norway is the ideal first test: same grid system (EEA), similar national taxonomy
(Artsnavnebasen/Nortaxa), red list available on GBIF.

**Steps:**

1. Fill in `config_NO.yml` TODOs (taxonomy DOI, red list DOI, GBIF cube DOIs)
2. Download data: GBIF cubes for NO, EEA grid clipped to Norway, Nortaxa DwC-A
3. Run full pipeline
4. Document any Sweden-specific assumptions that break

**Expected issues:**

- Column name differences in Nortaxa vs Dyntaxa DwC-A
- Red list format variations
- Taxonomy reconciliation differences (Nortaxa may have different synonym structure)
- Invasive species: no Norwegian checklist currently on GBIF as DwC-A (`invasives.enabled: false`)

## 2.2 Configuration Refinement

Based on Norway results, refine the config system:

- [ ] Taxonomy source abstraction — generic field mapping for any DwC-A backbone
- [ ] Red list abstraction — handle different threat status column names
- [ ] Feature flags — graceful degradation when components are missing:

```yaml
features:
  has_national_checklist: true   # false → use GBIF backbone
  has_redlist: true              # false → skip threat analysis
  has_invasives: true            # false → skip invasive flag
  has_regional_grids: true       # false → generate from country boundary
```

## 2.3 Grid System Abstraction

The `CELLCODE_FIELD` parameter is scaffolded but not yet fully implemented.

- [ ] Replace remaining `eeacellcode` references with `CELLCODE_FIELD`
- [ ] Abstract grid file format (currently assumes `.gpkg` with specific column names)
- [ ] Support ISEA3H hexagonal grids via `dggridR` package
- [ ] Auto-generate grid from country boundary when no pre-built grid exists


# Phase 3: Ethiopia & Global Grids

*Goal: Prove the pipeline works outside Europe with a fundamentally different grid system.*

## 3.1 ISEA3H Grid Integration

Ethiopia requires ISEA3H (Icosahedral Snyder Equal Area) hexagonal grids since
EEA grids only cover Europe.

**Technical approach:**

1. Generate ISEA3H hexagons covering Ethiopia at ~10km and ~50km resolutions
   using `dggridR`
2. Request GBIF occurrence cubes on ISEA3H grid for Ethiopia
3. Map `seqnum` (ISEA3H cell ID) to the generic `CELLCODE_FIELD`
4. Verify spatial analysis works with hexagonal cells

## 3.2 Taxonomy Without National Backbone

Ethiopia may not have a DwC-A national checklist. Options:

- Use GBIF backbone taxonomy filtered to Ethiopian species
- Use regional checklists (e.g., Flora of Ethiopia and Eritrea)
- Skip Tier 1–3 matching, rely on GBIF backbone + API directly

The pipeline should handle this gracefully via `features.has_national_checklist: false`.

## 3.3 Sparse Data Handling

Ethiopian GBIF data is likely much sparser than Sweden. The pipeline should:

- Adjust thresholds dynamically based on data volume
- Handle orders/families with very few records gracefully
- Skip analyses that require minimum data density


# Phase 4: R Package

*Goal: Transform the project into an installable R package.*

## 4.1 Package Structure

```
gbifgaps/
├── DESCRIPTION
├── NAMESPACE
├── R/
│   ├── config.R              # Config loading and validation
│   ├── download.R            # GBIF cube and taxonomy downloads
│   ├── ingest.R              # Grid, taxonomy, cube processing
│   ├── summarise.R           # Core and species-level summaries
│   ├── gaps_spatial.R        # Spatial gap analysis
│   ├── gaps_temporal.R       # Temporal gap analysis
│   ├── gaps_taxonomic.R      # Reconciliation + taxonomic gaps
│   ├── integrate.R           # Overview tables
│   ├── prepare_apps.R        # Shiny data bundling
│   └── utils.R               # safe_sum, safe_read, helpers
├── inst/
│   ├── shiny/
│   │   ├── gap_app/
│   │   └── gbif_explorer/
│   ├── configs/
│   │   ├── config_template.yml
│   │   ├── config_sweden.yml
│   │   └── config_norway.yml
│   ├── analysis/             # Rmd reports
│   └── targets/              # _targets.R template
├── vignettes/
│   ├── getting-started.Rmd
│   ├── configuration.Rmd
│   ├── running-analysis.Rmd
│   └── customization.Rmd
├── tests/
│   └── testthat/
└── man/
```

## 4.2 Core API

```r
library(gbifgaps)

# Full pipeline in one call
run_pipeline("config_sweden.yml")

# Or step by step
setup_project("config_sweden.yml")
download_data()
ingest_data()
make_summaries()
analyse_gaps()
prepare_apps()

# Launch dashboards
launch_gap_app()
launch_explorer_app()

# Render reports
render_reports()
```

## 4.3 Package Development Tasks

- [ ] Create `DESCRIPTION` with proper metadata, authors, license
- [ ] Extract reusable functions from scripts into `R/` files
- [ ] Write `roxygen2` documentation for all exported functions
- [ ] Create vignettes (Getting Started, Configuration, Running, Customization)
- [ ] Write unit tests (`testthat`) for config validation, data processing, matching
- [ ] Set up `pkgdown` documentation site
- [ ] GitHub Actions for R CMD check


# Phase 5: Distribution & Hosting

## 5.1 Package Distribution

- [ ] Host on GitHub (`gbif/gbifgaps` or `gbif-sweden/gbifgaps`)
- [ ] R CMD check passing on Linux, macOS, Windows
- [ ] `pkgdown` site with country gallery and screenshots
- [ ] Consider CRAN submission (later, once API is stable)

## 5.2 Shiny App Hosting

Options for GBIF nodes:

| Option | Effort | Cost | Best For |
|--------|--------|------|----------|
| shinyapps.io | Low | Free tier / $$ | Quick demos, small nodes |
| Posit Connect | Medium | $$$ | Enterprise nodes |
| ShinyProxy + Docker | Medium | Server costs | Self-hosted nodes |
| GBIF central hosting | Low (for nodes) | Central | `gaps.gbif.org/{country}` |

The central hosting model is most appealing: one deployment at GBIF Secretariat
with a country selector, nightly data refreshes, and consistent branding.

## 5.3 Data Refresh Strategy

- GBIF cubes can be re-downloaded periodically (monthly/quarterly)
- Taxonomy reconciliation cache persists across runs
- `targets` pipeline only reruns what changed
- Consider scheduled GitHub Actions for automated updates


# Phase 6: Future Enhancements

## Analysis Extensions

- [x] Sampling bias quantification (Troudet-style proportional sampling analysis)
- [x] Invasive species integration (national registries as DwC-A)
- [x] Restricted species integration (sensitive species list with generalization categories)
- [ ] Species distribution model coverage gaps
- [ ] Temporal trend predictions and forecasting
- [ ] Climate envelope coverage analysis (SMHI / Copernicus gridded data)
- [ ] Cross-country comparison mode

## App Features

- [x] Administrative boundary overlays on all maps
- [x] Cascading taxonomy filters across app tabs
- [x] Troudet-style sampling bias analysis with auto-drill
- [x] Rolling 12-month activity window
- [x] Region/municipality selector in Explorer app
- [x] Scope filter on taxonomic + threatened tabs (both apps)
- [x] Dyntaxa/All GBIF scope toggle
- [x] Kingdom-level spatial filtering on recency map
- [x] Heatmap log/linear/count scale toggle
- [x] Expandable About descriptions on every tab
- [x] Publisher tab with dependency analysis and mobilisation timeline
- [x] Active filter breadcrumb with clear button
- [x] Primates/configurable order exclusion
- [ ] Species of Concern tab (Threatened + Invasive + Sensitive sub-tabs)
- [ ] Troudet group exclusion multi-select
- [ ] GBIF-style default group mapping for Troudet landing view
- [ ] PhyloPic taxonomic silhouette icons on charts
- [ ] PDF report generation from within apps
- [ ] Data download buttons for all tables and maps
- [ ] Country comparison view (side-by-side dashboards)
- [ ] API endpoint for programmatic gap metric access
- [ ] Publisher type classification and colour coding

## Community

- [ ] Contribution guidelines (CONTRIBUTING.md)
- [ ] Country config repository (community-maintained configs)
- [ ] GBIF node webinars and tutorials
- [ ] Conference presentations (GBIF Science Symposium, TDWG)


# Open Questions

1. **Grid bundling** — Should the package bundle EEA grids or always download?
2. **GBIF authentication** — How to handle API credentials for cube downloads?
3. **Data freshness** — How often should analyses be re-run?
4. **Central hosting** — Will GBIF Secretariat host a multi-country instance?
5. **Branding** — Should apps be GBIF-branded or node-branded?
6. **1km resolution** — Is sub-10km resolution needed? Major architectural implications for data volume.
7. **Unicellular organisms** — Currently excluded from gap analysis (no Dyntaxa coverage). Monitor for future checklists.
8. **Restricted species** — ✅ Resolved: SLU Artdatabanken publishes a DwC-A checklist with sensitivity categories in `taxonRemarks`. Integrated as `is_sensitive` flag with `sensitivity_category` (5km/25km/50km).


# Timeline

| Quarter | Milestone |
|---------|-----------|
| Q1 2026 | Phase 1.1–1.3 complete — pipeline hardened, dual-scope, sensitive species |
| Q2 2026 | Phase 1.4–1.5 complete — all app tabs reviewed and polished |
| Q2 2026 | Phase 2 started — Norway replication |
| Q3 2026 | Phase 3 started — Ethiopia grid abstraction |
| Q3 2026 | Phase 4 — Package skeleton with core API |
| Q4 2026 | Phase 5 — First public release, pkgdown site |
| 2027    | Phase 6 — Community adoption, advanced features |

---

*This roadmap is a living document. Updated `r format(Sys.Date(), '%B %d, %Y')`.*
